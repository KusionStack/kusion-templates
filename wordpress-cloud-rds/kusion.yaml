projectName: wordpress-cloud-rds
description: A sample wordpress project with cloud rds
quickstart: kusion apply
projectFields:
  - name: AppName
    description: The Application Name. Default to 'wordpress'.
    type: string
    default: wordpress
  - name: ProjectName
    description: The Project Name. Default to 'wordpress'.
    type: string
    default: wordpress
stacks:
  - name: dev
    fields:
      - name: Image
        description: The Image Address. Default to 'wordpress:4.8-apache'.
        type: string
        default: wordpress:4.8-apache
