projectName: wordpress
description: A sample wordpress project
quickstart: kusion apply
projectFields:
  - name: AppName
    description: The Application Name. Default to 'wordpress'.
    type: string
    default: wordpress
  - name: ProjectName
    description: The Project Name. Default to 'wordpress'.
    type: string
    default: wordpress
stacks:
  - name: dev
    fields:
      - name: Image
        description: The Image Address. Default to 'wordpress:6.3'.
        type: string
        default: wordpress:6.3
